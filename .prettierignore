dist
original
playwright-report*
test-results
src/i18n/locales/
src/i18n/locales.generated.*
tests/visual/__screenshots__
package-lock.json

# Generated by scripts/generate-seo-locales.mjs and verified by `npm run verify:seo`, which
# regenerates and compares. Formatting them here would make that check fail on the next run.
*.html

# Prose, wrapped deliberately. Reformatting to printWidth reflows every paragraph, which buries
# the actual edits in a document's history for no gain.
*.md

# Written by `npm run verify:native-fixtures --write` from the C# effects, and compared byte for
# byte on every run. Reformatting it here would make that comparison a formatting check too.
tests/fixtures/native-effects.json
