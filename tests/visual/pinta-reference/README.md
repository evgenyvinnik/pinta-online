# Native Pinta screenshot references

Place manually captured screenshots from the original desktop Pinta application here. Each PNG must use the exact filename of the corresponding approved web screenshot in `../__screenshots__/chromium/`.

Run `npm run test:visual:review` to generate the side-by-side comparison gallery.
